# WorkSync MCP Server

MCP server that exposes WorkSync (mic-issues) backend API endpoints as tools.

## Features

- Create, list, and manage issues
- Assign/unassign issues to users
- Update issue status
- User management (CRUD operations)

## Usage

```bash
uv run python -m src.main
```

Server runs on http://127.0.0.1:9000
